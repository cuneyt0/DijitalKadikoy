import 'package:flutter/material.dart';

class PayMoney extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PayMoney Detail', style: TextStyle(fontSize: 30),),
      ),
    );
  }
}