import 'package:flutter/material.dart';

class UploadMoney extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Upload MoneyDetail', style: TextStyle(fontSize: 30),),
      ),
    );
  }
}