import 'package:flutter/material.dart';

class TakeTheSurvey extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Ankete Katıl sayfası', style: TextStyle(fontSize: 30),),
      ),
    );
  }
}