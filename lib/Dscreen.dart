import 'package:flutter/material.dart';

class ScreenD extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Screen D', style: TextStyle(fontSize: 30),),
    );
  }
}